#include "fileparser.h"


/*
// Assign the other parameters
		string temp;
		istringstream ss(scan);
		getline(ss, key, ',');
		getline(ss, temp, ',');
		time = stoi(temp);
		getline(ss, deadline, ',');
		getline(ss, temp, ',');
		priority = stoi(temp);
		getline(ss, temp, ',');
		numPartitions = stoi(temp);
		getline(ss, details, ',');
*/
